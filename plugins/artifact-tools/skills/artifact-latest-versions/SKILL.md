---
description: Show all available versions of a Stratio artifact: snapshot, releases, milestones, prereleases and dev branch.
model: haiku
allowed-tools: mcp__artifacts-manager__get_artifact_versions
---

Call `get_artifact_versions` with the artifact name "$ARGUMENTS" and reproduce the full response exactly as returned, without omitting, summarizing, or reformatting any field.
